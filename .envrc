# Auto-load the toolchain when you enter this repo, so your editor gets Rust language
# features (rust-analyzer, cargo) on the host. Requires direnv + nix-direnv.
# `nix develop` already picks the right shell per platform (editing tools on macOS,
# the full build toolchain on Linux), so plain `use flake` is all we need here.
use flake
