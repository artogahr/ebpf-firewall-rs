# Auto-load the host toolchain (rustc/cargo/rust-analyzer) when you enter this repo, so
# your editor gets Rust language features on the host. Requires direnv + nix-direnv.
# See the README "Editor autocomplete" section. (This is the host analysis toolchain;
# the actual build still happens in the guest via `nix run .#enter`.)
use flake .#analyzer
